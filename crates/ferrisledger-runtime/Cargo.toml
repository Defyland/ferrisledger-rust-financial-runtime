[package]
name = "ferrisledger-runtime"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
ferrisledger-domain = { path = "../ferrisledger-domain" }
ferrisledger-events = { path = "../ferrisledger-events" }
ferrisledger-index = { path = "../ferrisledger-index" }
ferrisledger-rules = { path = "../ferrisledger-rules" }
ferrisledger-store = { path = "../ferrisledger-store" }
serde.workspace = true
thiserror.workspace = true
time.workspace = true

[dev-dependencies]
criterion.workspace = true
tempfile.workspace = true

[[bench]]
name = "replay"
harness = false

[lints]
workspace = true
