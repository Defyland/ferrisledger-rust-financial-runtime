{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/Defyland/ferrisledger-rust-financial-runtime/docs/events/money_deposited.v1.json",
  "title": "money_deposited v1",
  "type": "object",
  "required": ["event_id", "event_type", "stream_id", "tenant_id", "correlation_id", "schema_version", "occurred_at", "producer", "payload"],
  "properties": {
    "event_id": { "type": "string" },
    "event_type": { "const": "money_deposited" },
    "stream_id": { "type": "string" },
    "tenant_id": { "type": "string" },
    "correlation_id": { "type": "string" },
    "causation_id": { "type": ["string", "null"] },
    "schema_version": { "type": "integer", "const": 1 },
    "occurred_at": { "type": "string", "format": "date-time" },
    "producer": { "type": "string", "const": "ferrisledger" },
    "payload": {
      "type": "object",
      "required": ["kind", "data"],
      "properties": {
        "kind": { "const": "money_deposited" },
        "data": {
          "type": "object",
          "required": ["tenant_id", "account_id", "amount", "idempotency_key"],
          "properties": {
            "tenant_id": { "type": "string" },
            "account_id": { "type": "string" },
            "amount": { "$ref": "#/$defs/money" },
            "idempotency_key": { "type": "string" }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    }
  },
  "$defs": {
    "money": {
      "type": "object",
      "required": ["cents", "currency"],
      "properties": {
        "cents": { "type": "integer", "minimum": 1 },
        "currency": { "type": "string", "minLength": 3, "maxLength": 3 }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
